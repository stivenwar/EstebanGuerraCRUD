export interface Categories{
  categories : [];
  id: number;
  // is_extended:boolean;
  // layout: number;
  name: string;
  // order: number;
  // published: boolean;
  products:[];
  display_name:string;
  reference_format:string;
  price_instruccions:[];
  share_url:string;
  thumnail:string;
  unit_price:string;






}
